# todov2
Simple Todo App Build with golang and nextjs

# Required Software
Docker

# Running
In Development mode
`docker compose -f docker-compose.dev.yml up --build`

In Production Mode
`docker compose up -d`